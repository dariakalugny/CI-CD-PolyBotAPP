apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-server-pvc
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/jenkins"
